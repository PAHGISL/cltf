% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{fit_cltf}
\alias{fit_cltf}
\title{Fit the CLTF model with deterministic multistart optimization}
\usage{
fit_cltf(
  observations,
  forcing,
  application_rate_g_ha,
  top_bulk_density_g_cm3,
  bottom_bulk_density_g_cm3,
  lower = c(mu = 0.05, sigma = 0.05, R_top = 0.1, R_bottom = 0.1, k = 0),
  upper = c(mu = 10, sigma = 3, R_top = 100, R_bottom = 100, k = 0.1),
  initial = c(mu = 1, sigma = 0.5, R_top = 2, R_bottom = 3, k = 0.005),
  starts = NULL,
  n_starts = 6L,
  seed = 42,
  top_thickness_mm = 100,
  bottom_thickness_mm = 200,
  effective_porosity = 0.2,
  method = c("adaptive", "trapezoid"),
  n_steps = 1001L,
  rel_tol = 1e-08,
  penalty = 1e+06,
  bound_tolerance = 1e-06,
  control = list(maxit = 500)
)
}
\arguments{
\item{observations}{Prepared observations containing analysis concentrations,
elapsed days, and explicit depth intervals.}

\item{forcing}{Data frame containing \code{time_days} and cumulative infiltration.}

\item{application_rate_g_ha}{Applied herbicide mass in grams per hectare.}

\item{top_bulk_density_g_cm3, bottom_bulk_density_g_cm3}{Layer bulk densities.}

\item{lower, upper}{Named parameter bounds.}

\item{initial}{Optional first optimization start.}

\item{starts}{Optional matrix of explicit starts, one per row.}

\item{n_starts}{Number of starts generated when \code{starts} is absent.}

\item{seed}{Random seed for generated starts.}

\item{top_thickness_mm, bottom_thickness_mm}{Model layer thicknesses.}

\item{effective_porosity}{Empirical concentration scale.}

\item{method, n_steps, rel_tol}{Convolution settings passed to \code{\link[=simulate_cltf]{simulate_cltf()}}.}

\item{penalty}{Finite objective returned for invalid model predictions.}

\item{bound_tolerance}{Relative tolerance for reporting bound hits.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
An \code{cltf_fit} list containing the best fit and all start results.
}
\description{
Fit the CLTF model with deterministic multistart optimization
}
